VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# Only needed when the API is on a different origin than the site (e.g. production).
# VITE_API_URL=https://your-api.example.com
# ALLOWED_ORIGIN=https://your-site.example.com   (server-side, enables CORS for that origin)
